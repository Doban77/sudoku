module.exports = function solveSudoku(matrix) {
  for (let col = 0; column < matrix.length; col++) {
    for (let row = 0; row < matrix.length; row++) {
      if (matrix[col][row] === 0) {

      }
    }
  }
}